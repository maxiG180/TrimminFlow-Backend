package com.trimminflow.demo.entity;

/**
 * DayOfWeek Enum
 *
 * Represents the days of the week for business hours
 * Using custom enum instead of java.time.DayOfWeek for better control
 */
public enum DayOfWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}
